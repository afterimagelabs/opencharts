node_modules
dist
dist-ssr
.DS_Store
*.local
.env
.env.*
!.env.example
.vite
tsconfig.tsbuildinfo
*.tsbuildinfo

# Toolkit PDFs / CSV are generated by scripts/build-toolkit.mjs at build time.
# They are NOT checked in; the deploy regenerates them on every push.
public/toolkit/
# Same with the OG image — rasterized from public/og.svg at build time.
public/og.png
